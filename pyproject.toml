[project]
name = "frost-depth-calculator"
version = "0.1.0"
description = "PySide6 and SQLite ground freezing depth calculator"
requires-python = ">=3.11"
dependencies = [
    "PySide6>=6.7",
    "PySide6-WebEngine>=6.7",
    "SQLAlchemy>=2.0",
]

[tool.ruff]
line-length = 120
